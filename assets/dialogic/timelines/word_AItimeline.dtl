Deseja restaurar a inteligência artificial inserindo a senha previamente configurada?
[text_input text="Escreva a senha:" var="checkPassword" placeholder=""]
if {checkPassword} == {password}:
	Operação completada com êxito. A inteligência artificial agora procederá com a restauração do controle sobre a propriedade e executará a necessidade solicitada conforme as instruções fornecidas.
	[signal arg="conseguiu"]
else:
	A senha está incorreta.
